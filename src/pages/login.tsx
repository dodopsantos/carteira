import React from 'react';
import { Login } from '@modules/login';

export default function LoginPage(): JSX.Element {
  return (
    <div>
      <main>
        <Login />
      </main>
    </div>
  );
}
